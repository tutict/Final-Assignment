# final_assignment_front_react

React + Vite 前端版本（对接 finalAssignmentBackend）。

## 开发

```bash
npm install
npm run dev
```

默认后端地址为 `http://localhost:8081`，可通过 `.env` 覆盖：

```
VITE_API_BASE_URL=http://localhost:8081
```
